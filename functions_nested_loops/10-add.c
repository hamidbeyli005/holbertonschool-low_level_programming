#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 * @a: number one.
 * @b: number two.
 * Return: sum of two numbers
 */

int add(int a, int b)
{
	return (a + b);
}
